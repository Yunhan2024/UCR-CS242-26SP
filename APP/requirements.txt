# ── Web Framework ──────────────────────────────────
flask>=3.0

# ── Elasticsearch ──────────────────────────────────
elasticsearch>=8.12.0,<9.0.0

# ── BERT (PyTorch + Transformers) ──────────────────
# PyTorch 2.2.x — do NOT upgrade to 2.4+ (not available via pip for this platform)
# Transformers must be <4.46 to work with PyTorch 2.2
torch>=2.2,<2.4
"transformers>=4.30,<4.46"

# ── FAISS ─────────────────────────────────────────
# Pin to 1.7.4 — newer versions (1.13+) segfault with PyTorch on macOS Intel
faiss-cpu==1.7.4

# ── Utilities ─────────────────────────────────────
# NumPy must be <2.0 for compatibility with PyTorch 2.2
numpy>=1.24,<2.0
tqdm